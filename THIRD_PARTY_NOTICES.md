# Third-Party Notices

## Runtime

The shipped game uses browser-native DOM, Canvas 2D, WebAudio, IndexedDB and Service Worker APIs. It does not load third-party scripts, fonts, analytics, advertising SDKs, or generative AI services at runtime.

## Build and test tooling

- Vite — MIT License
- TypeScript — Apache License 2.0
- Vitest — MIT License
- Playwright — Apache License 2.0

Their license texts and dependency metadata are distributed through the respective npm packages installed by `npm ci`.

## Reference material

The original GDD, source ZIP and 16 UI reference images are development inputs owned or supplied by the project owner. They are intentionally excluded from the public repository. The public game uses newly generated raster artwork and native DOM/CSS UI.

